from django.apps import AppConfig


class SimOrdersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.sim_orders"
    verbose_name = "SIM Orders & Activation Codes"
